#pragma once
#include "DeviceConfig.h"

// Copy this file to devices.h and fill in your entity IDs.
// devices.h is gitignored — never commit it.

constexpr DeviceEntry ACS[] = {
    { "climate.your_ac_entity", "AC Name" },
};
constexpr int AC_COUNT = sizeof(ACS) / sizeof(ACS[0]);
